function countToN(x)
{
    let absx = Math.abs(x);
    let index=1;
    while(index <= absx)
    {
        console.log(index);
        index=index+1; // index++;index+=1

    }
}

function Multuples(mult,m,n)
{
    let absx=Math.abs(n);
    let index = 1;
    while(inbex <= absx);
    { 
        mult.push(index*m);
        index=index+1;
    }
}